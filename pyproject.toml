[project]
name = "cd-ripper"
version = "0.1.0"
description = ""
authors = [
    {name = "Rob Fisher",email = "rob.fisher@outlook.com"}
]
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pyudev (>=0.24.4,<0.25.0)",
    "discid (>=1.4.0,<2.0.0)",
    "musicbrainzngs (>=0.7.1,<0.8.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
